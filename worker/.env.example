# Supabase — the worker writes ingested jobs with the service role.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Aggregator API keys (all optional — the worker uses whichever are present).
ADZUNA_APP_ID=
ADZUNA_APP_KEY=
JOOBLE_API_KEY=
JSEARCH_API_KEY=
